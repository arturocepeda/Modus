

 Once both repositories have been cloned or downloaded, make sure that the "ModusSamples" and
 "GameEngine" directories are placed sibling to the "Modus" directory, like this:
 
  <ParentDir>/Modus
  <ParentDir>/ModusSamples
  <ParentDir>/GameEngine
  
  